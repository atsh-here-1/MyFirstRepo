import { AlgorithmIdentifier } from "@peculiar/asn1-x509/build/types/algorithm_identifier";
import { TBSCertificate } from "@peculiar/asn1-x509/build/types/tbs_certificate";
/**
 * ```asn1
 * Certificate  ::=  SEQUENCE  {
 *   tbsCertificate       TBSCertificate,
 *   signatureAlgorithm   AlgorithmIdentifier,
 *   signatureValue       BIT STRING  }
 * ```
 */
export declare class Certificate {
    tbsCertificate: TBSCertificate;
    signatureAlgorithm: AlgorithmIdentifier;
    signatureValue: ArrayBuffer;
    constructor(params?: Partial<Certificate>);
}
